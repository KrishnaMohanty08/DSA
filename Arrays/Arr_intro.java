package Arrays;
import java.util.*;

public class arr_into {

    @SuppressWarnings("unused")
    public static void main(String[] args) {
        // Initializing Scanner for user input
        @SuppressWarnings("resource")
        Scanner sc = new Scanner(System.in);

        // Array Syntax
        // datatype[] arrayName = new datatype[size];
        // Example: int marks[] = new int[50];

        // Initializing an array with predefined values
        int[] numbers = {1, 2, 3, 5, 7, 9, 11, 24, 34, 13, 34, 5, 98};

        // Uncomment and modify as needed for user input functionality
        /*
        // Taking user input to populate the marks array
        int[] marks = new int[3];
        System.out.print("Enter marks for Physics: ");
        marks[0] = sc.nextInt();
        System.out.print("Enter marks for Chemistry: ");
        marks[1] = sc.nextInt();
        System.out.print("Enter marks for Maths: ");
        marks[2] = sc.nextInt();

        // Displaying entered marks
        System.out.println("Physics: " + marks[0]);
        System.out.println("Chemistry: " + marks[1]);
        System.out.println("Maths: " + marks[2]);

        // Updating a value in the array
        marks[2] = 100; // updating Maths marks
        System.out.println("Updated Maths marks: " + marks[2]);

        // Calculating percentage based on the marks array
        int percentage = (marks[0] + marks[1] + marks[2]) / 3;
        System.out.println("Percentage: " + percentage + "%");

        // Displaying the length of the array
        System.out.println("Length of marks array: " + marks.length);
        */

        // You can create a String array like this
        // String[] fruits = {"guava", "apple", "mango"};
    }
}
